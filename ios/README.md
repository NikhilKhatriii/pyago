# iOS platform folder

This Phase 1 delivery focuses on the Android Studio / Android target.
To generate the full iOS Xcode project scaffold, run:

```bash
flutter create --platforms=ios .
```

from the project root (with the Flutter SDK installed). This will not
touch any existing Dart code under `lib/`.
